name: flake

on:
  pull_request:
    branches:
      - master

jobs:
  check:
    runs-on: nix-builder
    steps:
      - uses: actions/checkout@v6
      - name: flake check all systems
        run: nix flake check --all-systems
